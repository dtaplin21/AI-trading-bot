# Backend environment variables
#
# Full master template (all API keys + frontend vars + labels):
#   ../../.env.example   (repo root)
#
# Copy the BACKEND sections from the root file into this file as .env

# See repo root .env.example for complete documentation.

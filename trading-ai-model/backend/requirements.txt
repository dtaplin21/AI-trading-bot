# requirements.txt
# Trading AI Model — full dependency list
# Python 3.11–3.13 required (3.14 blocked by numba via optional pandas-ta)

# ── HTTP + async ──────────────────────────────────────────────
httpx>=0.27.0          # Async HTTP — news API calls
aiohttp>=3.9.0         # Alternative async HTTP
asyncio-throttle>=1.0.0

# ── API ───────────────────────────────────────────────────────
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# ── Data validation ───────────────────────────────────────────
pydantic>=2.6.0        # All schemas (v2 required)
pydantic-settings>=2.2.0

# ── Database ──────────────────────────────────────────────────
asyncpg>=0.29.0        # Async Postgres / TimescaleDB
psycopg2-binary>=2.9.0 # Sync Postgres (TimescaleStore)
sqlalchemy>=2.0.0      # ORM layer
alembic>=1.13.0        # Migrations

# ── Data science ──────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.12.0

# ── ML models ─────────────────────────────────────────────────
lightgbm>=4.3.0        # Primary prediction model
xgboost>=2.0.0         # Alternate gradient boosting
scikit-learn>=1.4.0    # Preprocessing + evaluation
hmmlearn>=0.3.0        # Hidden Markov Model (Markov agent)

# ── Deep learning (Phase 3+) ──────────────────────────────────
# torch>=2.2.0          # Temporal Fusion Transformer
# pytorch-forecasting>=1.0.0

# ── Experiment tracking ───────────────────────────────────────
mlflow>=2.11.0

# ── Execution (Coinbase Advanced Trade) ───────────────────────
coinbase-advanced-py>=1.8.0

# ── Market data ───────────────────────────────────────────────
# pandas-ta: pip install -r requirements-ta.txt  (or pip install -e ".[ta]")
python-dotenv>=1.0.0   # .env loading

# ── Utilities ─────────────────────────────────────────────────
structlog>=24.1.0      # Structured JSON logging
tenacity>=8.2.0        # Retry logic
python-dateutil>=2.9.0
pytz>=2024.1
uuid6>=2022.10.25

# ── Testing ───────────────────────────────────────────────────
pytest>=8.1.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0
httpx[test]
